[build]
  command   = "npm run build"
  publish   = "dist"

[build.environment]
  NODE_VERSION = "20"

# Redirect all routes to index.html so React Router / SPA navigation works
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
